{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (splash only; the app itself runs from the local backend).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
