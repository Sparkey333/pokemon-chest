# Secrets — your BYOK API keys live here. NEVER commit this.
settings.local.json

# Cached TCGdex set data (re-fetched on demand)
scripts/cache/

# Cached PriceCharting catalog searches (re-fetched on demand)
search-cache.json

# Node / Tauri build artifacts
node_modules/
src-tauri/target/
src-tauri/gen/

# Built installers (kept locally in the project folder, not in git)
*.dmg
*.app

# OS cruft
.DS_Store

# Python bytecode
__pycache__/
*.pyc

# Generated iPhone snapshot (regenerated from data). The pattern only
# matched the pre-rename filename, so the Den-named build was untracked
# by luck rather than by rule.
Pok*mon *— Pocket.html
Pocket.html

# Generated reference/listing images
listing-photos/
